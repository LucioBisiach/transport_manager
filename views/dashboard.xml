<?xml version="1.0"?>
<odoo>

    <record id="act_check_week" model="ir.actions.act_window">
        <field name="name">Cheques de Terceros</field>
        <field name="res_model">account.check</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="dashboard_form_view" model="ir.ui.view">
        <field name="name">Dashboard Form</field>
        <field name="model">board.board</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form string="Dashboard">
                <board style="1-2">
                    <column>
                        <!-- <action
                            string="Cheques de Terceros"
                            name="%(transport_manager.act_check_week)d"/> -->
                    </column>
                    <column>

                    </column>
                </board>
            </form>
        </field>
    </record> 

    <record id="tms_finanzas_dashboard" model="ir.actions.act_window">
        <field name="name">Dashboard</field>
        <field name="res_model">board.board</field>
        <field name="view_mode">form</field>
        <field name="usage">menu</field>
        <field name="view_id" ref="dashboard_form_view"/>
    </record>

</odoo>